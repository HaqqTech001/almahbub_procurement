/**
 * Spacing / radius / color tokens for Version 2 UI.
 * CSS mirrors these as `--hamd-space-*` in `foundation.css`.
 */
export {
  space as spacingTokens,
  radius as radiusTokens,
  color as colorTokens,
  typography as typographyTokens,
  control as controlTokens,
  focusRing as focusRingTokens,
  motion as motionTokens,
  shadow as shadowTokens,
  font as fontTokens,
} from "@hamd/design-tokens";
