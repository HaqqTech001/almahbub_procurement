# Electronics/Mobile rollout status

Checked 2026-09-19. No products published, archived or deleted. No P2/P3 drafts created. No commit or push. Existing unrelated workspace changes were retained.

1. **P2028 diagnosis and fix.** The retained gimbal failure occurred while acquiring an interactive transaction. Generation, validation and storage were already outside that transaction. Remote pool latency/pressure is a plausible contributor, not a proven sole historical cause. The database factory did not dispose its externally supplied pg pool, and its retry extension could replay writes with uncertain outcomes. It now disposes the pool and retries only bounded, explicitly read-only operations. Pool size and timeouts were not raised. Draft attachment is one parameterized conditional SQL insert with a product row lock, draft/name/update-time checks, no-existing-image predicate and unique-conflict protection. No interactive transaction spans media work.

2. **Database health.** Read-only health passed: initial connection 1,334 ms, count reads 232-294 ms, draft reads 225-257 ms. Gimbal attachment took 264 ms. A later duplicate-scan query took 18,706 ms; remote latency is not eliminated. Stage diagnostics report safe stage names, durations and error codes. See `electronics-db-health.jsonl`.

3. **Durable storage.** Current driver is `local`. No complete Supabase Storage or S3 configuration is available in this API environment; production's intended driver is not established. Both supported durable implementations already flow through `createCatalogMediaStore`. No new vendor or environment changes were made. Local files survive a process restart on the same volume, but are not proven safe across Render redeploys. Current cards resize originals rather than storing separate thumbnails. `catalogue-durable-media-copy-plan.json` describes copying all four originals with unchanged keys, hashes, MIME and provenance, verifying remote copies, then conditional URL updates. The existing random-key `put()` is not a key-preserving migration tool; a reviewed copy extension is still required. No copy or old-binary deletion has occurred.

4. **Branded media review.** All eight P1 drafts have records in `electronics-branded-media-review.json`. Two candidates found: iPhone 17 and Galaxy Z Fold8. One rejected: Galaxy S26 Ultra retail-scene composition. Five currently lack an acceptable candidate. Manufacturer identity evidence does not grant image usage rights. Downloaded review files remain quarantined outside tracked source assets.

5. **Branded media approved.** Zero. Candidate presence and repository licenses do not constitute final card/detail, attribution and durable-import approval. No branded lookalikes generated.

6. **Gimbal correction.** Exactly one replacement generated after prompt dry-run. Visually reviewed complete handheld three-axis stabilizer with clamp, joints and grip, without the previous letter/logo issue. Attached to existing draft as ProductImage `4c91aa28-76a3-403f-a9eb-758957b41594`. SHA-256: `cc2b8b4fb0b712bfc2946330bfd44b7a8d1bbe40e89f4996428263ccc2b482bc`. Previous rejected candidate retained. Provenance and visual review updated. Still unpublished.

7. **Approval workflow.** `pnpm catalogue:approve-product --product=<slug>` is read-only by default. Only explicit `--execute` can approve one eligible P1 draft. It checks identity/category, current research where required, technical bytes, semantic review, provenance/rights, governed durable mapping, duplicate identities and unrelated binary associations. A conditional SQL statement atomically updates the draft and existing variant review metadata. Runtime catalogue reads persisted approval evidence and rechecks existing public gates. All four generic dry-runs report only `durable_storage_required`. No approval execute command was run. Current technical approval supports fully decoded PNGs; non-PNG branded candidates fail closed until a validated decoder/review path is added.

8. **Generic P1 publicly approved.** Zero. Power bank, USB-C charger, wireless earbuds and corrected gimbal have reviewed valid draft media. All await durable copying and fresh individual approval checks.

9. **Branded P1 publicly approved.** Zero. The first branded publication remains conditional on completing generic rollout and its own media gates.

10. **Total PUBLIC_APPROVED.** Zero. Legacy published database statuses were not restored to public visibility.

11. **Category page.** Desktop 1440 and mobile 390 checks: zero cards, correct loaded hero, legacy URL redirects to canonical category, no placeholder text or horizontal overflow. View More opens the canonical category filter and preserves it after reload. See `electronics-rollout-browser-results.json` and screenshots. Approved-card visual checks cannot be completed before an actual approval.

12. **Product detail.** All four generic draft public endpoints return 404. This verifies draft gating, not an approved detail page. Public image/name/category/detail presentation must be checked after individual approval.

13. **Commodities.** Cashew and cocoa subject candidates were downloaded, hashed and visually inspected. Shea is source-identified but reference-only because of scene composition. Source/license/author/hash/dimensions and exact commodity mapping are in `commodity-media-correction-queue.json`. Attribution, final composition approval and durable import remain outstanding. No commodity records or active images were changed.

14. **Audit.** Complete bounded audit passed and atomically replaced its report after successful traversal and count checks. 1,134 total; 1,024 classified retainable; 175 with technically valid media; 849 needing media; 48 already archived; 110 archive proposals including those 48; zero duplicate/delete candidates; zero broken or unverified media rows. These counts are not public semantic approvals. The before-attachment snapshot had 174 with media and 850 missing; only the gimbal attachment changed those counts. See `electronics-rollout-audit-comparison.json`. No audit mutation took place.

15. **Files in this task.** Code: `database/index.ts`, `database/vitest.config.ts`, `database/pool-lifecycle.test.ts`; catalogue application `attach-draft-primary.ts`, `catalogue-stage.ts`, `persisted-publication-review.ts`, `catalog-service.ts`; catalogue media `draft-catalogue-media.ts`; scripts `catalogue-db-health.ts`, `audit-product-catalogue.ts`, `attach-reviewed-catalogue-candidate.ts`, `approve-catalogue-product.ts`, `validate-product-catalogue.ts`; root `package.json`. Tests: catalogue `catalog-service`, `category-preview`, `catalogue-audit`, `product-priority`, `persisted-publication-review`; Integrated Export `ie-commodity-routes`. Reports: provenance, generic/branded visual reviews, commodity correction queue, durable copy plan, four approval reports, health/audit/validation logs, audit snapshot/comparison, browser results/screenshots and this report. Some files already contained earlier uncommitted work; this is a task inventory, not a claim that their entire Git diff belongs to this task. Temporary browser runners are removed after validation.

16. **Validation.** API and Web TypeScript checks passed. Web production build passed. Relevant API catalogue/Integrated Export tests: 92 passed. Relevant Web tests: 27 passed. Database pool tests: 4 passed. Focused API lint passed. Full API lint remains failed with 14 existing errors outside this rollout's implementation: seven `no-explicit-any` in auth-controller tests, four `URL` no-undef in build-product-media-queue.mjs, and three duplicate branches in generate-product-primary-media.ts. Four old catalogue test failures were resolved by adding approved fixtures while preserving pagination/media-health assertions. Git whitespace check passed. Browser checks, health, four approval dry-runs and complete audit passed within the limitations above.

17. **Remaining blockers.** Configure one supported durable public-media provider through environment/Render settings without sharing secrets in chat. Review and implement the key-preserving non-destructive copy plan, verify durable reads, and rerun individual approvals. Obtain acceptable branded photography and documented usage rights, add supported technical validation for any non-PNG candidates, and perform approved card/detail desktop/mobile checks after publication. Complete commodity attribution/composition/durable review. Investigate remaining remote latency and resolve the separate full-lint failures before a clean release gate. No publication standard was bypassed to make the category nonempty.
